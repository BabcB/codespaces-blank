const isprogram = true;

const arrowfunc = (perev) => {
  {
    const blockit = null;
    // debugger;
    console.log(blockit);
  }

  console.log(perev);
};

function normalfunc(perv) {
  {
    const blockit = null;
    debugger;
    console.log(blockit);
  }
  console.log(perv);
}

arrowfunc();

normalfunc();
